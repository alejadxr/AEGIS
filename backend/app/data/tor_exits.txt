# TOR exit node IP list — refresh from https://check.torproject.org/exit-addresses on startup if AEGIS_AI_MODE != disabled
# Format: one IP per line, lines starting with # are comments
# This is a stub — populate via network refresh or replace with a full list
