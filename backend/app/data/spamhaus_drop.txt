# Spamhaus DROP (Don't Route Or Peer) list — refresh from https://www.spamhaus.org/drop/drop.txt on startup if AEGIS_AI_MODE != disabled
# Format: CIDR notation, one entry per line, lines starting with # are comments
# This is a stub — populate via network refresh or replace with a full list
